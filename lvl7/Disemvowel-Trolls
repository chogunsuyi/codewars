function disemvowel(str) {
  return str.split('').filter(item => {
    return item.toLowerCase() !== 'a' &&
      item.toLowerCase() !== 'e' &&
      item.toLowerCase() !== 'i' &&
      item.toLowerCase() !== 'o' &&
      item.toLowerCase() !== 'u'
  }).join('');
}
